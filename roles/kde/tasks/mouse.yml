- name: Mouse Speed
  kconfig:
    file: ~/.config/kcminputrc
    groups: [Mouse]
    key: "{{ item.key }}"
    value: "{{ item.value }}"
  with_dict:
    XLbInptAccelProfileFlat: "true"
    XLbInptPointerAcceleration: "1"
